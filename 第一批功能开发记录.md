# 第一批功能开发记录

> 开发时间: 2026-02-11  
> 提交: `bb559e8` → `main`  
> 变更: 7 files, +511 / -21

## 功能清单

### 1. 主题切换器 (Theme Switcher)
- **文件**: `src/components/Header.jsx`
- **实现**: 在导航栏右侧添加 ThemeSwitcher 组件，支持 System / Light / Dark 三档
- **原理**: 通过 `document.documentElement.setAttribute('data-theme', theme)` 切换 CSS 变量集
- **持久化**: `localStorage.setItem('theme', theme)`
- **CSS 基础**: 项目已有 `[data-theme="dark"]` 和 `@media (prefers-color-scheme: dark)` 样式定义

### 2. 多种排序方式 + 排序方向
- **文件**: `server/index.js`, `src/pages/HomePage.jsx`
- **后端**: `filterSkills()` 函数扩展，接收 `sort` (newest/updated/downloads/name) 和 `order` (asc/desc) 参数
- **前端**: 排序下拉框 + 升降序切换按钮 (↑↓)
- **API**: `GET /api/skills?sort=name&order=asc`

### 3. Highlighted 过滤器
- **文件**: `server/index.js`, `src/pages/HomePage.jsx`
- **实现**: 在工具栏添加 "⭐ Highlighted" 切换按钮
- **逻辑**: `highlighted=true` 时只返回有下载记录的 skills
- **API**: `GET /api/skills?highlighted=true`

### 4. 卡片/列表视图切换
- **文件**: `src/pages/HomePage.jsx`, `src/components/SkillCard.jsx`, `src/index.css`
- **实现**: Cards (▦) / List (☰) 视图切换按钮组
- **Cards 模式**: 原有网格布局 (`skills-grid`)
- **List 模式**: 垂直列表 (`skills-list`)，SkillCard 横向排列

### 5. 卡片指标展示
- **文件**: `server/index.js`, `src/components/SkillCard.jsx`, `src/index.css`
- **后端**: API 返回的 skills 中合并 `downloadCount` 和 `versionCount` 字段
- **前端**: 卡片底部显示 ↓ 下载量 和 ↑ 版本数 (大数字自动格式化: 1000+ → 1k)

### 6. License 信息显示
- **文件**: `server/github-sync.js`, `src/pages/SkillPage.jsx`, `src/index.css`
- **后端**: `parseSkillMd()` 解析 frontmatter 时提取 `license` 字段
- **前端**: 详情页 meta 区域显示 📜 License 徽章

## 修改文件明细

| 文件 | 改动 |
|------|------|
| `src/components/Header.jsx` | +ThemeSwitcher 组件 |
| `src/pages/HomePage.jsx` | +排序/过滤/视图工具栏 |
| `src/components/SkillCard.jsx` | +指标展示 + 列表模式 |
| `src/pages/SkillPage.jsx` | +License 徽章 |
| `server/index.js` | +排序/过滤/下载量合并 API |
| `server/github-sync.js` | +license 字段提取 |
| `src/index.css` | +全部新增样式 (~285 行) |
