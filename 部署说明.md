┌─────────────────────────────────────────────────────────────┐
│                      同步策略架构                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   首次部署                      后续更新                     │
│   ─────────                     ─────────                   │
│   npm run sync                  GitHub Webhook              │
│   (全量同步)                    (增量同步)                   │
│       │                              │                       │
│       ▼                              ▼                       │
│   ┌─────────┐                  ┌──────────┐                 │
│   │ 获取全部 │                  │ 仅同步变更 │                │
│   │ Skills  │                  │ 的 Skills │                │
│   └─────────┘                  └──────────┘                 │
│       │                              │                       │
│       └──────────┬───────────────────┘                       │
│                  ▼                                           │
│           skills-cache.json                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘




🔧 配置 GitHub Webhook（增量同步）
步骤 1：配置 Webhook Secret
在 .env 文件中添加：

env
GITHUB_TOKEN=your_github_token_here
WEBHOOK_SECRET=your_random_secret_string
步骤 2：在 GitHub 仓库设置 Webhook
进入 openclaw/skills 仓库（或您有权限的 fork）
Settings → Webhooks → Add webhook
配置：
Payload URL: https://www.ccc.onl/api/webhook
Content type: application/json
Secret: 与 .env 中的 WEBHOOK_SECRET 一致
Events: 选择 "Just the push event"
步骤 3：工作流程
阶段	操作	API 请求量
首次部署	npm run sync	一次性全量
日常更新	Webhook 触发	仅变更文件
手动刷新	POST /api/sync	按需全量



📋 部署清单
## 首次部署
- [ ] 克隆项目
- [ ] npm install
- [ ] 配置 .env (GITHUB_TOKEN, WEBHOOK_SECRET)
- [ ] npm run sync (首次全量同步)
- [ ] npm run build && npm start

## 生产环境配置
- [ ] 配置 GitHub Webhook
- [ ] 配置反向代理 (nginx)
- [ ] 配置 SSL 证书