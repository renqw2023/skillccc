# 启动说明

## 🚀 快速启动（推荐）

**同时启动前后端**：
```bash
cd e:\skillccc
npm run dev
```

这会同时启动：
- 🔵 后端 API 服务器：http://localhost:3001
- 🟢 前端开发服务器：http://localhost:5173

---

## 📝 分别启动（调试用）

### 启动后端
```bash
cd e:\skillccc
npm run dev:server
```
- 运行在：http://localhost:3001
- 自动同步：每日3点全量同步，每2小时增量同步

### 启动前端
```bash
cd e:\skillccc
npm run dev:client
```
- 运行在：http://localhost:5173
- 支持热更新（HMR）

---

## 🛑 停止服务

在运行服务的终端按 `Ctrl+C` 停止

---

## ✅ 验证是否成功启动

### 方法1：浏览器访问
- 前端：http://localhost:5173
- 后端：http://localhost:3001/api/health

### 方法2：命令行测试
```powershell
# 测试前端
Invoke-WebRequest http://localhost:5173

# 测试后端健康检查
Invoke-WebRequest http://localhost:3001/api/health

# 测试 Highlighted Skills API
Invoke-WebRequest http://localhost:3001/api/skills/highlighted
```

---

## 🔧 常见问题

### 端口被占用
如果提示端口被占用（EADDRINUSE），先停止所有Node进程：

**Windows:**
```powershell
# 查找占用端口的进程
netstat -ano | findstr :3001
netstat -ano | findstr :5173

# 停止进程（替换 PID 为实际进程ID）
taskkill /PID <进程ID> /F
```

### 服务器无法启动
1. 确保在项目根目录 `e:\skillccc`
2. 检查 Node.js 版本：`node --version`（推荐 v18+）
3. 重新安装依赖：`npm install`

---

**创建时间**: 2026-02-10 21:44
